export interface NavItem {
    label: string,
    route: string
}

export interface Category {
    id: string,
    name: string
}

export interface Post {
    id: string,
    author: string,
    title: string,
    img: string,
    likes: string
}